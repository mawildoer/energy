from setuptools import setup

setup(
    name="energy",
    version="0.1",
    author="",
    license="GPLv3",
    packages=["energy"],
    install_requires=[""],
    package_data={"energy": ["*.tif", "*.csv", "*.yml"]}
)
